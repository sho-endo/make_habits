<div class="row">
  <div class="col-xs-6 col-xs-offset-3">
    <div class="panel panel-warning">
      <div class="panel-heading">
        <h2 class="panel-title"><%= @quit.title %></h2>
      </div>
      <div class="panel-body">
        <ul>
          <% @rules.each do |rule| %>
            <li><h3><%= rule.content %></h3></li>
          <% end %>
        </ul>
      </div>
    </div>
    <%= link_to "一覧に戻る", current_user, class: "btn btn-success" %>
  </div>
</div>